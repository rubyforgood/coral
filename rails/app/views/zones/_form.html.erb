<%= bootstrap_form_with(model: zone, local: true,
                        bootstrap: { layout: 'horizontal' }) do |form| %>

  <%= form.text_field :name %>

  <%= form.submit class: 'btn-primary' do %>
    <%= link_to "Cancel", zones_path, class: 'btn btn-link' %>
  <% end %>
<% end %>
